---
title: useDeleteApiKey
description: Delete an API key for the current user.
---

Requires the Better Auth API key plugin.

## Usage

```tsx
import { useAuth } from "@better-auth-ui/react"
import { useDeleteApiKey } from "@better-auth-ui/react/plugins/api-key"

const { authClient } = useAuth()
const mutation = useDeleteApiKey(authClient)

mutation.mutate(/* params */)
```

## Options factory

```tsx
import { deleteApiKeyOptions } from "@better-auth-ui/react/plugins/api-key"
import { useMutation } from "@tanstack/react-query"

const mutation = useMutation(deleteApiKeyOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/hooks/mutations/use-delete-api-key.ts" name="DeleteApiKeyParams" />
