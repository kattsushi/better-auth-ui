---
title: useRemoveMember
description: Remove a member from an organization.
---

Organization mutations require `organizationClient()` on the Better Auth client.

## Usage

```tsx
import { useAuth } from "@better-auth-ui/react"
import { useRemoveMember } from "@better-auth-ui/react/plugins/organization"

const { authClient } = useAuth()
const mutation = useRemoveMember(authClient)

mutation.mutate(/* params */)
```

## Options factory

```tsx
import { removeMemberOptions } from "@better-auth-ui/react/plugins/organization"
import { useMutation } from "@tanstack/react-query"

const mutation = useMutation(removeMemberOptions(authClient))
```

## Params

<auto-type-table path="../../../../../../packages/react/src/hooks/mutations/use-remove-member.ts" name="RemoveMemberParams" />
