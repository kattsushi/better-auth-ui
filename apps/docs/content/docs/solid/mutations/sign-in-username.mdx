---
title: signInUsernameOptions
description: Username/password sign-in mutation options.
---

Requires the Better Auth username plugin. Import your configured `authClient`; it already carries the username methods from `createAuthClient`.

## Options factory

```tsx
import { signInUsernameOptions } from "@better-auth-ui/core/plugins/username"
import { createMutation } from "@tanstack/solid-query"

const signIn = createMutation(() => signInUsernameOptions(authClient))

signIn.mutate({ username: "alice", password: passwordInput })
```

## Params

<auto-type-table path="../../../../../../packages/core/src/plugins/username/sign-in-username-mutation.ts" name="SignInUsernameParams" />
